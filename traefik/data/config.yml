http:
 #region routers
  routers:
#endregion
#region services
  services:
#endregion
  middlewares:
    pocketid-headers:
      headers:
        customResponseHeaders:
          X-Frame-Options: "SAMEORIGIN"
          X-Content-Type-Options: "nosniff"
          Referrer-Policy: "strict-origin-when-cross-origin"
          # Leave Content-Security-Policy unset
